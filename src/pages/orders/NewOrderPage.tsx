import { zodResolver } from '@hookform/resolvers/zod'
import { useQuery,useQueryClient } from '@tanstack/react-query'
import { ArrowLeft,LoaderCircle,MapPin,PackageSearch,Plus,ShoppingBasket,Trash2,UserRound } from 'lucide-react'
import { useFieldArray,useForm,useWatch } from 'react-hook-form'
import { Link,useNavigate } from 'react-router-dom'
import { useAuth } from '../../features/auth/auth-context'
import { listProducts } from '../../features/catalog/catalog.api'
import { orderFormSchema,type OrderForm } from '../../features/orders/order-form.schema'
import { createOrder,listOpenTabs } from '../../features/orders/orders.api'
import { ApiError } from '../../lib/api'
const money=(value:number)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(value)
export function NewOrderPage(){const{currentEstablishment}=useAuth();const establishmentId=currentEstablishment?.id;const navigate=useNavigate();const client=useQueryClient();const products=useQuery({queryKey:['products','order-form'],queryFn:()=>listProducts({active:true})});const tabs=useQuery({queryKey:['tabs','open',establishmentId],queryFn:()=>listOpenTabs(establishmentId!),enabled:Boolean(establishmentId)});const{register,control,handleSubmit,setValue,formState:{errors,isSubmitting}}=useForm<OrderForm>({resolver:zodResolver(orderFormSchema),defaultValues:{type:'DINE_IN',tabId:'',customerName:'',customerPhone:'',street:'',number:'',neighborhood:'',city:'',notes:'',items:[{productId:'',productVariantId:'',quantity:'1',notes:''}]}});const{fields,append,remove}=useFieldArray({control,name:'items'});const type=useWatch({control,name:'type'});const items=useWatch({control,name:'items'});const total=(items??[]).reduce((sum,item)=>{const product=products.data?.find(entry=>entry.id===item.productId);const variant=product?.variants.find(entry=>entry.id===item.productVariantId);return sum+(Number(product?.basePrice??0)+Number(variant?.priceAdjustment??0))*Number(item.quantity.replace(',','.')||0)},0);const submit=handleSubmit(async values=>{if(!establishmentId)return;try{const order=await createOrder({establishmentId,type:values.type,...(values.tabId&&{tabId:values.tabId}),...(values.notes&&{notes:values.notes}),...(values.customerName&&{customer:{name:values.customerName,...(values.customerPhone&&{phone:values.customerPhone})}}),...(values.type==='DELIVERY'&&{deliveryAddress:{street:values.street,number:values.number,neighborhood:values.neighborhood,city:values.city}}),discount:0,fees:0,items:values.items.map(item=>({productId:item.productId,...(item.productVariantId&&{productVariantId:item.productVariantId}),quantity:Number(item.quantity.replace(',','.')),options:[],discount:0,...(item.notes&&{notes:item.notes})}))});await client.invalidateQueries({queryKey:['orders']});navigate(`/pedidos/${order.id}`,{replace:true})}catch(reason){setValue('root.serverError',{type:'server',message:reason instanceof ApiError?reason.message:'Não foi possível criar o pedido.'})}});return <form className="order-editor page-enter" onSubmit={submit} noValidate><Link className="back-link" to="/pedidos"><ArrowLeft size={16}/> Voltar aos pedidos</Link><div className="order-editor-heading"><span className="eyebrow">Novo atendimento</span><h1>Novo pedido</h1><p>Monte o pedido e revise os valores antes de confirmar.</p></div>{errors.root?.serverError&&<div className="form-error" role="alert">{errors.root.serverError.message}</div>}<div className="order-editor-grid"><div className="order-builder"><section><header><span><UserRound size={18}/></span><div><strong>1. Atendimento</strong><small>Canal, cliente, mesa ou endereço</small></div></header><div className="order-type-options">{(['DINE_IN','COUNTER','TAKEAWAY','DELIVERY'] as const).map((value)=><label className={type===value?'selected':''} key={value}><input type="radio" value={value} {...register('type')}/>{value==='DINE_IN'?'Salão':value==='COUNTER'?'Balcão':value==='TAKEAWAY'?'Retirada':'Delivery'}</label>)}</div>{type==='DINE_IN'&&<label className="builder-field">Comanda aberta<select {...register('tabId')}><option value="">Selecione uma comanda</option>{(tabs.data??[]).map(tab=><option value={tab.id} key={tab.id}>{tab.label||`Comanda ${tab.id.slice(0,8)}`}{tab.table?.number?` · Mesa ${tab.table.number}`:''}</option>)}</select><FieldError message={errors.tabId?.message}/></label>}<div className="customer-fields"><label className="builder-field">Cliente <small>Opcional</small><input {...register('customerName')} placeholder="Nome do cliente"/></label><label className="builder-field">Telefone <small>Opcional</small><input {...register('customerPhone')} placeholder="(00) 00000-0000"/></label></div>{type==='DELIVERY'&&<div className="delivery-fields"><label className="builder-field field-wide"><MapPin size={15}/> Rua<input {...register('street')} placeholder="Nome da rua"/><FieldError message={errors.street?.message}/></label><label className="builder-field">Número<input {...register('number')} placeholder="123"/><FieldError message={errors.number?.message}/></label><label className="builder-field">Bairro<input {...register('neighborhood')} placeholder="Bairro"/><FieldError message={errors.neighborhood?.message}/></label><label className="builder-field">Cidade<input {...register('city')} placeholder="Cidade"/><FieldError message={errors.city?.message}/></label></div>}</section><section><header><span><PackageSearch size={18}/></span><div><strong>2. Produtos</strong><small>Itens e variações do catálogo</small></div></header><div className="order-items-form">{fields.map((field,index)=>{const product=products.data?.find(item=>item.id===items?.[index]?.productId);return <article key={field.id}><div className="item-line"><label className="builder-field">Produto<select {...register(`items.${index}.productId`)} onChange={event=>{setValue(`items.${index}.productId`,event.target.value,{shouldValidate:true});setValue(`items.${index}.productVariantId`,'')}}><option value="">Selecione o produto</option>{(products.data??[]).map(item=><option value={item.id} key={item.id}>{item.name} · {money(Number(item.basePrice))}</option>)}</select><FieldError message={errors.items?.[index]?.productId?.message}/></label><label className="builder-field">Variação<select {...register(`items.${index}.productVariantId`)} disabled={!product?.variants.length}><option value="">Padrão</option>{product?.variants.filter(item=>item.active).map(item=><option value={item.id} key={item.id}>{item.name} ({Number(item.priceAdjustment)>=0?'+ ':''}{money(Number(item.priceAdjustment))})</option>)}</select></label><label className="builder-field quantity-field">Quantidade<input {...register(`items.${index}.quantity`)} inputMode="decimal"/><FieldError message={errors.items?.[index]?.quantity?.message}/></label><button type="button" className="remove-item" onClick={()=>remove(index)} disabled={fields.length===1}><Trash2 size={17}/></button></div><label className="builder-field">Observação do item <small>Opcional</small><input {...register(`items.${index}.notes`)} placeholder="Ex.: sem cebola"/></label></article>})}</div><button type="button" className="add-item-button" onClick={()=>append({productId:'',productVariantId:'',quantity:'1',notes:''})}><Plus size={16}/> Adicionar outro item</button></section><label className="builder-field order-notes-field">Observações gerais <small>Opcional</small><textarea {...register('notes')} placeholder="Informações para a cozinha ou atendimento"/></label></div><aside className="order-summary"><header><ShoppingBasket size={19}/><strong>Resumo do pedido</strong></header><div className="summary-items">{items?.filter(item=>item.productId).map((item,index)=>{const product=products.data?.find(entry=>entry.id===item.productId);return <div key={`${item.productId}-${index}`}><span>{item.quantity}× {product?.name}</span><strong>{money((Number(product?.basePrice??0)+Number(product?.variants.find(entry=>entry.id===item.productVariantId)?.priceAdjustment??0))*Number(item.quantity.replace(',','.')||0))}</strong></div>})}</div><dl><div><dt>Subtotal</dt><dd>{money(total)}</dd></div><div><dt>Adicionais</dt><dd>R$ 0,00</dd></div><div><dt>Descontos</dt><dd>R$ 0,00</dd></div><div className="summary-total"><dt>Total</dt><dd>{money(total)}</dd></div></dl><button className="primary-button" disabled={isSubmitting||!establishmentId}>{isSubmitting?<><LoaderCircle className="spin" size={18}/> Criando pedido...</>:<>Confirmar pedido <ShoppingBasket size={18}/></>}</button></aside></div></form>}
function FieldError({message}:{message?:string}){return message?<small className="field-error">{message}</small>:null}
